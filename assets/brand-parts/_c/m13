ults { display: grid; gap: 0; }
.result, .card, .notice, .empty {
  padding: 20px 0;
  border-bottom: 1px solid var(--line);
  background: transparent;
  box-shadow: none;
}
.result h3, .result a { font-family: var(--serif); font-size: 22px; text-decoration: none; }
.result p { color: var(--muted); margin: 8px 0 0; }
.badge {
  display: inline-block;
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  border: 0;
  background: transparent;
  padding: 0;
  margin-bottom: 6px;
}

/* About */
.m-about { padding: 56px 0 24px; }
.m-about-mark {
  width: min(280px, 70vw);
  height: auto;
  object-fit: contain;
  object-position: center;
  margin: 0 0 28px;
}
.m-about-mark img {
  width: 100%;
  height: auto;
  object-fit: contain;
  object-position: center;
  display: block;
  image-rendering: auto;
}
.m-about-contact { padding: 0 0 64px; }
.m-about-contact h2 { margin: 0 0 12px; font-size: 22px; }
.m-about-contact p { margin: 0 0 8px; color: var